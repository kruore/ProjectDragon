using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DamagePopupManager : MonoBehaviour
{
    //[SerializeField] Transform pfDamagePopup;
    //[SerializeField] Transform damagePopupTransform;
    //DamagePopup damagePopup;
    // Start is called before the first frame update
    void Start()
    {
        //damagePopupTransform = Instantiate(pfDamagePopup, Vector3.zero, Quaternion.identity);
        //damagePopup = damagePopupTransform.GetComponent<DamagePopup>();

        //damagePopup.Setup(300);

        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
